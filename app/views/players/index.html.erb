<h1>Das Konsumspiel</h1>
<p>Warst du einkaufen? Erzähl uns von deinen Sünden:</p>

<div class="container-centered container">
  <div class="button btn-yellow-lg">
    <%= link_to "Jetzt eintragen", user_items_new_path %>
  </div>
</div>

<div class="flex-container">
<% for i in 0..34 do %>
  <div class=
    <%= if i < 6
      "field"
    elsif i < 12
      "field-s"
    elsif i < 18
      "field-md"
    elsif i < 24
      "field-lg"
    elsif i < 30
      "field-xl"
    else
      "field-xxl"
    end %>>
  <div class=<%= i < 18 ? "number" : "number-lg" %>><%=i*10%></div>
  <% for player in @players.each do %>
    <% if player.points.round(-1) == i * 10 %>
    <div class="center mt-4">
      <%= cl_image_tag(player.photoname, class: "icon-img") %>
      <div class="name"><%= player.first_name %></div>
    </div>
    <% end %>
  <% end %>
  </div>
<% end %>
<div class="field-xxl">
  <%= cl_image_tag("rauch_xasbwz.png", class: "gameover-img") %>
  <p>Game Over</p>
</div>
</div>







